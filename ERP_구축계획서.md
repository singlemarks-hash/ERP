# 작은따옴표 ERP 시스템 구축 계획서

- 작성일: 2026-08-02
- 버전: v1.0
- 대상: 전 직원 (대표 / 경영지원본부 / 오프라인사업부 / 온라인사업부)
- 관리 주체: 경영지원본부 (시스템 총괄 관리자)

---

## 1. 개요

### 1.1 목적
사내에 흩어져 있는 업무 도구(급여, 연차, 각종 웹 툴)를 하나의 포털로 통합한다.
직원은 한 번의 로그인으로 자신에게 허용된 모든 기능에 접근하고,
경영지원본부는 모든 행정 처리(직원 등록, 권한 부여, 급여·연차 데이터 관리, 홈 버튼 URL 관리)를
단일 관리자 화면에서 총괄한다.

### 1.2 구축 범위 (1차)
| 모듈 | 내용 | 사용 대상 |
|---|---|---|
| 로그인 | 부서 + 이름 선택 → 비밀번호 입력. 최초 로그인 시 비밀번호 설정 | 전 직원 |
| 홈 | 사내 툴 바로가기 버튼 (공용 버튼: 경영지원본부만 편집 / 개인 버튼: 본인이 자유롭게 추가) | 전 직원 |
| 급여이력 | 본인 급여 지급 내역 조회 (월별 리스트 + 상세 명세, 토스형 UI) | 전 직원 (관리자 포함, 본인분만) |
| 급여관리 | 전 직원 월별 급여 데이터 입력·관리 (3.3% / 사대보험 / 아티스트 구분) | 경영지원본부 |
| 연차/휴가 관리 | 직원별 할당 연차, 사용일수, 잔여일수 조회·관리 | 본인분: 전 직원 / 전체: 경영지원본부·대표 |
| 설정 | 이메일 알람 수신 토글 (1차는 토글 UI + 설정 저장까지) | 전 직원 |
| 직원 관리 | 직원 등록/수정, 부서 배정, 비밀번호 초기화 | 경영지원본부 |
| 권한 모니터링 | 직원별 권한 현황 + 활동(로그인, 권한 변경, 데이터 수정) 로그 | 경영지원본부 |

### 1.3 전제 및 참고
- 기존 급여대장 대시보드(https://payroll-3719e.web.app/)와 같은 Firebase Hosting + Firestore 구조를 따른다.
- 직원 명단은 시스템에 미리 심지 않는다. **최초 실행 시 경영지원본부 관리자 1명을 등록**하고,
  이후 관리자가 [직원 관리] 화면에서 전 직원을 직접 등록한다.
- 주민등록번호 등 고유식별정보는 시스템에 **저장하지 않는다** (개인정보보호법 준수).

---

## 2. 시스템 아키텍처 (SaaS 구조)

### 2.1 구성도

```
[직원 브라우저 (PC/모바일)]
        │  HTTPS
        ▼
[Firebase Hosting]  ← 정적 SPA (HTML/CSS/JS)
        │
        ├── [Firebase Authentication] ─ 익명 세션 발급 (Firestore 접근 토큰용)
        │
        └── [Cloud Firestore] ─ 단일 데이터베이스
              ├ employees      직원 마스터 (비밀번호 해시 포함)
              ├ homeButtons    공용 바로가기 버튼 (관리자 소유)
              ├ personalButtons/{직원ID}  개인 버튼
              ├ payroll/{YYYY-MM}/rows   월별 급여 데이터
              ├ leaves         직원별 연차 (할당/사용 기록)
              ├ settings       직원별 알람 설정
              └ auditLogs      활동 로그 (권한 모니터링용)
```

- **멀티테넌트 확장 고려**: 1차는 단일 회사(작은따옴표) 전용이지만, 모든 컬렉션 경로를
  `companies/{companyId}/...` 하위로 옮기면 그대로 SaaS 멀티테넌트로 확장 가능한 구조로 설계한다.
  (1차는 루트 컬렉션 사용, 경로 상수를 코드 한 곳(`js/firebase-config.js`)에 모아둔다.)
- **서버리스**: 별도 백엔드 서버 없이 Firebase 관리형 서비스만 사용 → 운영비 최소화 (Spark 무료 플랜으로 시작 가능).

### 2.2 기술 스택
| 계층 | 선택 | 이유 |
|---|---|---|
| 프론트엔드 | 순수 HTML/CSS/JS (SPA) | 빌드 없이 배포 가능, 기존 payroll 앱과 동일한 운영 방식 |
| 호스팅 | Firebase Hosting | 기존 인프라와 동일, HTTPS/CDN 기본 제공 |
| DB | Cloud Firestore | 실시간 반영, 보안 규칙, 무료 플랜 |
| 인증 | Firebase Anonymous Auth + 자체 비밀번호 검증 | "부서+이름+비밀번호" 커스텀 로그인 요구사항 충족 |
| 비밀번호 저장 | SHA-256 + 직원별 salt 해시 | 평문 저장 금지 |

---

## 3. 로그인 시스템 상세 설계

### 3.1 로그인 플로우 (ID 없음)
```
① 부서 선택   [대표 | 경영지원본부 | 오프라인사업부 | 온라인사업부]
② 이름 선택   (선택한 부서의 재직 직원 목록이 드롭다운으로 표시)
③ 비밀번호 입력
      │
      ├─ 비밀번호 미설정 직원(passwordHash 없음)
      │     → "설정된 비밀번호가 없습니다. 비밀번호를 설정해주세요." 알림 표시
      │     → [새 비밀번호] + [비밀번호 확인] 두 입력이 정확히 일치할 때만
      │        salt+해시를 DB(employees 문서)에 저장 → 즉시 로그인
      │     → 이후 로그인부터는 이 비밀번호를 계속 사용
      │
      └─ 비밀번호 설정된 직원 → 해시 대조 → 일치 시 로그인 / 불일치 시 오류
```

### 3.2 규칙
- 비밀번호 최소 4자 이상(내부 툴 기준, 관리자 정책으로 상향 가능).
- 비밀번호 분실 시: 경영지원본부가 [직원 관리]에서 **비밀번호 초기화** → 해당 직원은 다음 로그인 때 재설정.
- 로그인 성공/실패, 비밀번호 설정/초기화는 모두 `auditLogs`에 기록.
- 세션: 브라우저 localStorage에 로그인 상태 저장(로그아웃 시 삭제).

### 3.3 최초 실행 (부트스트랩)
- 직원 데이터가 하나도 없으면 로그인 화면에 **[시스템 초기 설정]**이 나타난다.
- 여기서 경영지원본부 소속 최초 관리자 1명(이름+비밀번호)을 등록하면
  그 계정이 자동으로 관리자 권한(admin)을 가지며, 이후 모든 직원 등록은 이 계정으로 진행한다.

---

## 4. 권한 체계

### 4.1 역할 정의
| 역할 | 부여 대상 | 권한 |
|---|---|---|
| **admin (총괄 관리자)** | 경영지원본부 소속 전원 (자동) | 모든 기능. 직원 등록/수정, 권한 부여, 공용 버튼 URL 편집, 전체 급여·연차 열람/편집, 권한 모니터링, 활동 로그 열람 |
| **executive (임원 열람)** | 대표 부서 소속 | 전체 급여·연차 **열람** (편집 불가), 그 외 일반 기능 |
| **member (일반)** | 오프라인사업부·온라인사업부 | 본인 급여·연차 열람, 개인 버튼 관리, 본인 설정 |

- 역할은 부서 배정 시 자동 부여되며(경영지원본부→admin, 대표→executive, 사업부→member),
  경영지원본부가 [직원 관리]에서 개별 직원 단위로 수동 조정할 수도 있다.
- 공용 홈 버튼의 URL은 admin만 생성/수정/삭제 가능. 다른 계정에서는 편집 UI 자체가 렌더링되지 않고,
  Firestore 데이터 상으로도 수정 이력이 auditLogs에 남는다.

### 4.2 권한 모니터링 (경영지원본부 전용)
- **권한 현황판**: 전 직원 × (부서, 역할, 비밀번호 설정 여부, 최근 로그인, 재직 상태) 매트릭스.
- **활동 로그**: 로그인, 비밀번호 설정/초기화, 직원 등록/수정, 역할 변경, 공용 버튼 편집,
  급여·연차 데이터 수정이 시간순으로 기록되어 필터링 조회 가능.

---

## 5. 화면(메뉴) 상세 설계

### 5.1 공통 레이아웃
```
┌────────────┬──────────────────────────────┐
│ [사용자 카드]  │                              │
│  이름 / 부서   │                              │
│  직급 / 이메일 │        메인 콘텐츠 영역          │
│  권한 배지     │                              │
├────────────┤                              │
│ 홈           │                              │
│ 급여대장       │                              │
│ 연차/휴가 관리  │                              │
│ 설정          │                              │
│ ─ 관리자 ─    │                              │
│ 직원 관리      │  (경영지원본부에게만 표시)         │
│ 권한 모니터링   │                              │
├────────────┤                              │
│ 로그아웃       │                              │
└────────────┴──────────────────────────────┘
```
- 좌측 상단 사용자 카드: 이름, 부서, 직급, 이메일, 입사일, 권한 배지를 모두 표기.
- 모바일에서는 사이드바가 상단 드로어로 전환.

### 5.2 홈
- **공용 바로가기**: 사내 툴 버튼 그리드 (이름, URL, 설명, 색상). 새 탭으로 열림.
  admin만 [버튼 추가/편집/삭제] 가능 — URL 소유권은 경영지원본부에 있음.
- **내 바로가기**: 직원 개인이 자유롭게 추가/편집/삭제하는 버튼 영역. 본인에게만 보인다.

### 5.3 급여이력 (전 직원)
- 본인 급여만 표시. 최근 지급월의 금액을 크게 보여주는 히어로 + 연도별 월 리스트 (토스형 UI).
- 월을 누르면 상세 명세 모달: 세전금액, 식대, 추가 수당, 공제 합계, 실수령액, 비고.
- 관리자도 이 화면에서는 본인 급여만 본다 (관리자 역시 급여를 받는 직원이므로).

### 5.3-1 급여관리 (경영지원본부 전용, 관리자 메뉴)
- 상단 월 선택 탭 (1월 ~ 12월, 연도 전환).
- 기존 급여대장 구조를 반영한 구분: **3.3% (사업소득) / 사대보험 / 아티스트**.
- 표 컬럼: 구분, 이름, 세전금액, 식대, 추가 수당, 세후금액, 비고. 행 추가/수정/삭제.
- 상단 요약 카드: 해당 월 총 인건비, 인원수, 구분별 합계.

### 5.4 연차/휴가 관리
- 내 연차 카드: **할당 연차 / 사용 일수 / 잔여 일수** 3개 수치 + 사용 내역 리스트.
- admin/executive: 전 직원 연차 현황 테이블 (할당/사용/잔여, 진행률 바).
- admin: 직원별 할당 일수 설정, 사용 기록 추가/삭제 (날짜, 일수 0.5 단위, 유형: 연차/반차/병가/경조 등).

### 5.5 설정
- **이메일 알람 수신 토글** 1개 (급여·연차·공지 업데이트 메일 수신 여부).
  1차는 토글 상태를 직원별로 DB 저장까지 구현. 실제 메일 발송은 2차(아래 로드맵).

### 5.6 직원 관리 (admin 전용)
- 직원 등록: 이름, 부서, 직급, 이메일, 입사일, 고용 구분(정직원/3.3%/아티스트), 재직 상태.
- 수정/퇴사 처리, 역할(권한) 변경, 비밀번호 초기화.

---

## 6. 데이터 모델 (Firestore)

| 컬렉션/문서 | 주요 필드 | 비고 |
|---|---|---|
| `employees/{id}` | name, dept, position, email, joinDate, empType, status(재직/퇴사), role, salt, passwordHash, lastLoginAt | 주민번호 등 고유식별정보 저장 금지 |
| `homeButtons/{id}` | label, url, desc, color, order | admin만 쓰기 |
| `personalButtons/{empId}` | items[{label,url,color}] | 본인만 쓰기 |
| `payroll/{YYYY-MM}/rows/{id}` | category(3.3%/사대보험/아티스트), empId?, name, gross, meal, extra, net, note | 급여 숫자는 DB에만 존재 (코드/저장소에 미포함) |
| `leaves/{empId}` | allocated, records[{date, days, type, note}] | 잔여 = allocated − Σdays |
| `settings/{empId}` | emailNotif(bool) | |
| `auditLogs/{id}` | ts, actorId, actorName, action, detail | 권한 모니터링 데이터 원천 |

### 보안 규칙 (firestore.rules) 방침
- 모든 읽기/쓰기는 Firebase 익명 인증 세션이 있어야 가능 (외부 무단 접근 차단).
- 비밀번호는 salt 해시로만 저장. 급여 데이터는 저장소(GitHub)에 절대 포함하지 않는다.
- **한계와 보완 계획**: 1차 구조는 클라이언트 검증 기반이라 내부자가 규칙을 우회할 여지가 있다.
  2차에서 Cloud Functions + Custom Claims 로 역할을 토큰에 심어 서버 측에서 강제한다. (§8 로드맵)

---

## 7. 배포 절차

1. Firebase 콘솔에서 새 프로젝트 생성 (예: `quote-erp`) → 웹 앱 등록.
2. Firestore 활성화(프로덕션 모드) + Authentication에서 **익명 로그인** 활성화.
3. `erp/js/firebase-config.js`에 콘솔이 발급한 config 값 입력.
4. `firebase deploy` (Hosting: `erp/` 폴더, Rules: `firestore.rules`).
5. 최초 접속 → [시스템 초기 설정]에서 경영지원본부 관리자 등록.
6. 관리자 로그인 → [직원 관리]에서 급여대장 기준 전 직원 등록 → 각 직원에게 접속 안내.
   (직원은 첫 로그인 때 본인 비밀번호를 직접 설정)

---

## 8. 구축 로드맵

| 단계 | 내용 | 상태 |
|---|---|---|
| 1차 (본 계획) | 로그인/권한, 홈 버튼, 급여대장, 연차, 설정 토글, 직원 관리, 권한 모니터링 | **본 저장소에 구현 완료** |
| 2차 | 이메일 알람 실제 발송 (Cloud Functions + 메일 서비스), Custom Claims 기반 서버측 권한 강제 | 예정 |
| 3차 | 연차 신청→승인 워크플로, 급여명세서 PDF 발급, 기존 payroll 앱 데이터 이관/통합 | 예정 |
| 4차 | 멀티테넌트(SaaS) 전환: companies/{id} 스코프, 요금제/온보딩 | 선택 |

---

## 9. 운영 정책 요약

- **개인정보**: 주민등록번호 미수집. 급여 데이터는 Firestore에만 존재하며 접근 권한은 역할 기반 제한.
- **백업**: 경영지원본부가 월 1회 Firestore 내보내기(콘솔) 권장.
- **책임**: 시스템 관리(직원/권한/공용 버튼/데이터)는 경영지원본부가 총괄한다.
